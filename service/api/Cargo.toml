[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenvy = "0.15"
lambda_http = "1.0.0"
native-tls = "0.2"
postgres-native-tls = "0.5"
tokio-postgres = "0.7"

tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
